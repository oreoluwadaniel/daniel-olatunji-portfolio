import Link from "next/link";
import { allProjects } from "@/lib/portfolio-data";

const siteUrl = "https://daniel-olatunji-portfolio.vercel.app";
export const metadata = {title:"Projects",description:"Fifteen data analysis and business intelligence case studies covering revenue, retail, credit risk, insurance, experimentation, inventory, data quality, forecasting, SaaS, logistics and operations.",alternates:{canonical:siteUrl+"/work"},openGraph:{title:"Projects | Daniel Olatunji",description:"Fifteen data analysis and business intelligence case studies with findings, methods and limits stated clearly.",url:siteUrl+"/work",images:["/og-image.png"]}};

export default function WorkPage(){return <main><section className="section"><div className="container"><div className="eyebrow">Case studies</div><h1>Fifteen projects. Each one starts with a business question or a data problem.</h1><p className="lead">Each case study states the question, the data, the work, the result and the judgement behind it. Several report a negative result, because those were the results. Where a public repository exists, it is linked.</p><div className="project-grid">{allProjects.map(p=><article className="card project-card" key={p.slug}><Link href={`/work/${p.slug}`} className="cover-link"><img className="cover" src={p.cover} alt={`${p.title}: chart summary of the analysis`} width="1600" height="900" loading="lazy"/></Link><div className="content"><div className="meta">{p.category}</div><h2 className="card-title"><Link href={`/work/${p.slug}`}>{p.title}</Link></h2><p>{p.short}</p><div className="small">{p.scale}</div><div className="tag-list">{p.tools.map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="card-actions"><Link className="btn btn-primary" href={`/work/${p.slug}`}>Read the case study</Link>{p.github && p.github !== "https://github.com/oreoluwadaniel" && <a className="btn btn-secondary" href={p.github} target="_blank" rel="noreferrer">Repository</a>}</div></div></article>)}</div></div></section></main>}
