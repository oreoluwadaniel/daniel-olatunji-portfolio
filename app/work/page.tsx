import Link from "next/link";
import { allProjects } from "@/lib/portfolio-data";

const siteUrl = "https://daniel-olatunji-portfolio.vercel.app";
export const metadata = {title:"Projects",description:"Five data and business intelligence case studies covering revenue, inventory and working capital, product experimentation, forecasting and data quality. Each one states the numbers and what they mean.",alternates:{canonical:siteUrl+"/work"},openGraph:{title:"Projects | Daniel Olatunji",description:"Five case studies covering revenue, inventory, experimentation, forecasting and data quality, each with the numbers and the decision they support.",url:siteUrl+"/work",images:["/og-image.png"]}};

export default function WorkPage(){return <main><section className="section"><div className="container"><div className="eyebrow">Case studies</div><h1>Five projects. Each one ends in a decision somebody has to make.</h1><p className="lead">Every case study opens with the situation, states what the numbers showed, and says what to do about it. Two of them report a result that went against what was expected, because that is what the evidence said. The code and documentation behind each one is public.</p><div className="project-grid">{allProjects.map(p=><article className="card project-card" key={p.slug}><Link href={`/work/${p.slug}`} className="cover-link"><img className="cover" src={p.cover} alt={`${p.title}: visual summary`} width="1600" height="900" loading="lazy"/></Link><div className="content"><div className="meta">{p.category}</div><h2 className="card-title"><Link href={`/work/${p.slug}`}>{p.title}</Link></h2><p>{p.short}</p><div className="small">{p.scale}</div><div className="tag-list">{p.tools.map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="card-actions"><Link className="btn btn-primary" href={`/work/${p.slug}`}>Read the case study</Link><a className="btn btn-secondary" href={p.github} target="_blank" rel="noreferrer">Repository</a></div></div></article>)}</div></div></section></main>}
