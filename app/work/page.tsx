import Link from "next/link";
import { allProjects } from "@/lib/portfolio-data";

const siteUrl = "https://daniel-olatunji-portfolio.vercel.app";
export const metadata = {title:"Projects",description:"Four data and business intelligence case studies covering revenue, product experimentation, forecasting and data quality. Each one focuses on the business decision the analysis supports.",alternates:{canonical:siteUrl+"/work"},openGraph:{title:"Projects | Daniel Olatunji",description:"Four case studies covering revenue, experimentation, forecasting and data quality, with the evidence and decisions behind each one.",url:siteUrl}};

export default function WorkPage(){return <main><section className="section"><div className="container"><div className="eyebrow">Case studies</div><h1>Projects and decisions.</h1><p className="lead">Each case focuses on the business situation, the analysis, the evidence and the decision it supports. The code and documentation behind each one is public.</p><div className="project-grid">{allProjects.map(p=><article className="card project-card" key={p.slug}><Link href={`/work/${p.slug}`} className="cover-link"><img className="cover" src={p.cover} alt={p.title} width="1600" height="900" loading="lazy"/></Link><div className="content"><div className="meta">{p.category}</div><h2 className="card-title"><Link href={`/work/${p.slug}`}>{p.title}</Link></h2><p>{p.short}</p><div className="small">{p.scale}</div><div className="tag-list">{p.tools.map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="card-actions"><Link className="btn btn-primary" href={`/work/${p.slug}`}>Read the case study</Link><a className="btn btn-secondary" href={p.github} target="_blank" rel="noreferrer">Repository</a></div></div></article>)}</div></div></section></main>}
